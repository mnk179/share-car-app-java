import * as React from "react";

export class ProfileButton extends React.Component {
    render(){
        return(
            <div>
            <button>Profile</button>
            </div>
        );
    }
}