import * as React from "react";
import { Link } from "react-router-dom";

export class ChangeRoleButton extends React.Component {
    render(){
        return(
            <div>
            <button>Change role</button>
            </div>
        );
    }
}